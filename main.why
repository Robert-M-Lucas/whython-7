struct A {
    a: int,
    b: int,
    c: int
}

fn test() ~ A {
    return @A { 1, 2, 3 };
}

fn test_two() ~ int {
    return 103;
}

fn main() ~ int {
    \let t: A = test();
\
    \printi(t.a);
    \printi(t.b);
    \printi(t.c);

    let t_t: int = test_two();

    printi(t_t);

    return 7;
}