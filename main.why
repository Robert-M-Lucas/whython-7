fn main() ~ int {
    let i: int = 0;
    while (i < 30) {
        printi(i);
        printi(fibb(0, 1, i));
        i += 1;
    };

    return 1;
}

fn fibb(a: int, b: int, n: int) ~ int {
    if (n <= 0) { return a; };
    return fibb(b, a + b, n - 1);
}