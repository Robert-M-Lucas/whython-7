fn main() ~ int {
    let i: int = 0;

    while (i < 100) {
        printi(i);
        if ((i % 3) == 0) {
            printb(true);
        };
        if ((i % 5) == 0) {
            printb(false);
        };
        i += 1;
    };

    return 0;
}

