struct A {
    a: int,
    b: int,
    c: int
}

fn test(a: A) {
    printi(a.a);
    printi(a.b);
    printi(a.c);
}

fn main() ~ int {
    let a: A = @A { 1, 2, 3 };
    test(a);
    test(a);

    return 7;
}